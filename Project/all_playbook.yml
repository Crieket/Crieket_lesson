---
- hosts: all
  tasks:


- name:
  hosts: all
  become: true
  become_method: sudo
  vars_files:
  - /home/kas/Рабочий стол/Crieket_lesson/Project/all.yml
  ignore_errors: no 
  tasks:

  roles:
  - role: common-sys
  
- name:
  hosts: backend
  become: true
  become_method: sudo
  vars_files:
  - /home/kas/Рабочий стол/Crieket_lesson/Project/all.yml
  ignore_errors: yes
  tasks:

  roles: 
  - role: nginx 
  - role: php

- name:
  hosts: web
  become: true
  become_method: sudo
  vars_files:
  - /home/kas/Рабочий стол/Crieket_lesson/Project/all.yml
  ignore_errors: no 
  tasks:

- name:
  hosts: db
  become: true
  become_method: sudo
  vars_files:
  - /home/kas/Рабочий стол/Crieket_lesson/Project/all.yml
  ignore_errors: no 
  tasks:

  roles: 
  - role: mysql


#  - { role: logserv }
#  - { role: monitor }
#  - { role: backup: }


