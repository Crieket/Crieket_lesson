---
# - hosts: all
#   tasks:


- name:
  hosts: all
  become: true
  become_method: sudo
  vars_files:
  - /home/kas/Рабочий стол/Crieket_lesson/Project/all.yml
  ignore_errors: yes
  tasks:

  roles:
  - role: common-sys
  - role: nfs
  
- name:
  hosts: nginx
  become: true
  become_method: sudo
  vars_files:
  - /home/kas/Рабочий стол/Crieket_lesson/Project/all.yml
  ignore_errors: no
  tasks:

  roles: 
  - role: nginx 
  

- name:
  hosts: wp
  become: true
  become_method: sudo
  vars_files:
  - /home/kas/Рабочий стол/Crieket_lesson/Project/all.yml
  ignore_errors: no 
  tasks:
  
  roles:
  - role: wordpress
  - role: php


- name:
  hosts: db
  become: true
  become_method: sudo
  vars_files:
  - /home/kas/Рабочий стол/Crieket_lesson/Project/all.yml
  ignore_errors: no 
  tasks:

  roles: 
  - role: mysql


   


#  - { role: logserv }
#  - { role: monitor }
#  - { role: backup: }


